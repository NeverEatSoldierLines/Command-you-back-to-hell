{
  "name": "webstudyapp",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-mac": "electron-packager . webstudyapp --platform=darwin --electron-version=10.0.0 --arch=x64 --download.mirrorOptions.mirror=https://npm.taobao.org/mirrors/electron/ --asar --app-version=0.0.0 --build-version=1.0.0 --out=webstudyapp --overwrite --no-package-manager --ignore='(.git)' --icon=assets/img/icons/favicon.png"
  },
  "author": "sunhao",
  "license": "ISC",
  "dependencies": {
    "electron": "^12.0.5",
    "save-dev": "*"
  },
  "description": ""
}
